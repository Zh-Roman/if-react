import styled from 'styled-components';

export const StyleSliderWithFourItems = styled.div`
  margin: ${(props) => props.resetMargin};
  max-width: 1232px;
  position: relative;
`;
export const WithoutSlider = styled.div`
  display: flex;
  justify-content: space-around;
`;
